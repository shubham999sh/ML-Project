#include<stdio.h>
int main()
{
 int i,key,first,last,a[10],n,r;
first = a[i];
printf("enter the numbert of elements in the array:");
scanf("%d",&n);
 printf("enter the array:");
 scanf("%d",&a[i]);
 printf("enter the no which you want to find:");
 scanf("%d",&key);
 for(i=a[i];i<=n;i++)
  {
	if(key==a[i])
	{
		printf("***number succesfully find***");
	}
	printf("%d",i);
  }
 return 0;
}
